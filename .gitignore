# Dependencies
node_modules/

# Environment variables (secrets — never commit these)
.env
.env.local
.env.*.local

# Firebase service account key (if anyone ever saves the raw JSON in the repo by mistake)
*firebase-adminsdk*.json
serviceAccountKey.json

# Build outputs
dist/
build/
.next/

# Prisma
prisma/migrations/dev.db

# User-uploaded files (Resource post attachments) — stored on disk, not in git
Backend-Folder/uploads/

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json