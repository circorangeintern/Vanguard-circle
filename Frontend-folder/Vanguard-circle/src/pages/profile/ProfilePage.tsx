import ProfileSection from "../../components/dashboard/profile/sections/ProfileSection";

const ProfilePage = () => {
  return (
    <section className="min-h-screen bg-[var(--color-background)]">
      <ProfileSection />
    </section>
  );
};

export default ProfilePage;
